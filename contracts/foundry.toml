[profile.default]
src = "src"
out = "out"
libs = ["lib"]
# Deploy-determinism: the money contracts (^0.8.20 src) MUST compile under ONE pinned
# solc so the deployed bytecode is never ambiguous. Do NOT re-enable auto_detect_solc on
# the default (deploy) profile — the real-v4 RealSettlement test imports v4-core's real
# PoolManager (exact pragma 0.8.26), which would silently float the default compiler off
# 0.8.28. That regression is what noesis-120 fixes: the RealSettlement test is skip-excluded
# here and runs under the companion config `foundry.v4.toml` (auto-detect) instead.
solc = "0.8.28"
optimizer = true
optimizer_runs = 100
via_ir = true
evm_version = "cancun"
gas_reports = ["*"]
test = "test"
script = "script"
broadcast = "broadcast"
fs_permissions = [{ access = "read-write", path = "./deployments/" }]
# The RealSettlement regression imports v4-core's real PoolManager (exact pragma 0.8.26),
# which cannot compile under the pinned 0.8.28 default. Exclude it from the default compile
# set; it is exercised via `FOUNDRY_CONFIG=foundry.v4.toml forge test --match-path ...`.
skip = ["test/hooks/UniAlignmentV4Hook_RealSettlement.t.sol"]
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "solady/=lib/solady/src/",
    "solmate/=lib/solmate/",
    "v4-core/=lib/v4-core/src/",
    "v4-core/test/=lib/v4-core/test/",
    "dn404/=lib/dn404/",
    "@uniswap/v3-core/=lib/v3-core/contracts/",
    "@uniswap/v3-periphery/=lib/v3-periphery/contracts/",
    "@uniswap/v2-core/=lib/v2-core/contracts/",
    "zamm/=lib/zamm/src/",
    "zRouter/=lib/zRouter/src/",
    "createx-forge/=lib/createx-forge/",
    "aave-address-book/=lib/aave-address-book/src/"
]

# fuzz/invariant run counts live under their own tables (`[fuzz] runs` / `[invariant] runs`),
# not as flat `fuzz_runs`/`invariant_runs` keys — the flat form is silently ignored (forge
# emits an unknown-key warning), so CI was never actually running the elevated counts.
[profile.ci.fuzz]
runs = 1000

[profile.ci.invariant]
runs = 10000

[profile.fork]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.28"
optimizer = true
optimizer_runs = 100
via_ir = true
evm_version = "cancun"

# Fork configuration
# eth_rpc_url should be passed via --fork-url flag or loaded from .env
# fork_block_number = 21000000 # Old block, V4 liquidity limited
# Use latest block for V4 testing (December 2025)
gas_limit = 30000000
verbosity = 2

# Only run fork tests with this profile
match_path = "test/fork/**/*.sol"

[fmt]
line_length = 120
tab_width = 4
bracket_spacing = true
