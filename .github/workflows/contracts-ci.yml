name: contracts-ci

# Path-filtered: every step below runs with working-directory: contracts and reads only
# contracts/** (forge install/build/test, the EIP-170 gate script, both foundry.toml
# profiles). app-ci is intentionally left unfiltered — its G7 step rebuilds contracts
# from source and diffs the generated wagmi bindings, so a contracts-only change must
# still run app-ci to catch bindings drift (app-ci.yml G7). This filter is one-directional
# for that reason. `main` currently has no branch protection (no required checks), so an
# absent run here reads as not-applicable rather than a stuck "Expected" check; revisit
# this filter if branch protection is ever added.
#
# Shape: one `build` job compiles and runs every source-level gate, then `test` fans the
# ci-profile suite out across the shards in contracts/scripts/test-shards.json, each on its
# own hosted runner. Before sharding the single job took 39-48 minutes on a 4-vCPU runner:
# 14.6 of them compiling (via_ir, 434 files) and 22.9 running tests, of which one file,
# BondingCurveInvariant.t.sol, is more CPU than every other suite together. The shards are
# partitioned on measured CPU-seconds, and scripts/test-shards-cover.sh proves in the build
# job that every test file is in exactly one of them. The compiled tree moves from build to
# the shards through actions/cache under one content-addressed key, so a shard compiles
# nothing; the same cache, restored by prefix, is what makes the build step incremental
# from one push to the next instead of a cold 14-minute compile.
#
# Everything runs on GitHub-hosted runners. This is a public repository, and a self-hosted
# runner would execute any pull request's workflow on our own machine; GitHub's guidance is
# not to, and this workflow follows it.
on:
  push:
    paths:
      - 'contracts/**'
      - '.github/workflows/contracts-ci.yml'
  pull_request:
    paths:
      - 'contracts/**'
      - '.github/workflows/contracts-ci.yml'

defaults:
  run:
    working-directory: contracts

env:
  # Foundry PINNED to v1.5.1 to match app-ci.yml's G7: `--skip` glob semantics
  # differ across releases, and nightly reds where v1.5.1 is green. Keep the pin
  # identical so this job and G7 agree on the same toolchain behavior.
  FOUNDRY_VERSION: v1.5.1

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      shards: ${{ steps.shards.outputs.shards }}
      cache_key: ${{ steps.cache-key.outputs.key }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: ${{ env.FOUNDRY_VERSION }}

      # contracts/lib is gitignored with NO committed submodule gitlinks, so
      # actions/checkout fetches nothing and every dependency has to be cloned here.
      # The revisions live in contracts/scripts/lib-pins.txt and are pinned to exact
      # commits, so this gate compiles the same dependency code on every run and an
      # upstream push cannot red it. `all` is the full set: the 11 that src/ needs plus
      # forge-std and aave-address-book, which `forge test` imports and a src-only build
      # does not. app-ci's G7 step installs the `src` subset from the same file, so the
      # two jobs cannot drift apart.
      - name: Install contracts/lib deps
        run: ./scripts/install-libs.sh all

      - name: Format check
        run: forge fmt --check

      # INV-1 (docs/RISK_REGISTER.md, contracts/docs/spec/ZROUTER_AMOUNTLIMIT.md): zRouter is a
      # shared permissionless router and every leg pulls its input with transferFrom, so any
      # allowance left standing on it is spendable by any later caller. The gate refuses an
      # unbounded approval, one not sized to the swap it guards, one whose swap is in another
      # transaction, and one paired with an exact-out leg. --self-test runs first, against one
      # fixture per shape: a gate that stopped detecting a shape must red the build rather than
      # pass quietly. Placed before the build because it is a source scan -- it needs no
      # dependencies and no compiler, and failing it early costs nothing.
      - name: zRouter allowance gate (INV-1)
        run: |
          ./scripts/zrouter-approval-gate.sh --self-test
          ./scripts/zrouter-approval-gate.sh

      # `vm.expectRevert` arms the next call frame, and Solidity evaluates arguments before the
      # call -- so an external read left inside the guarded call's argument list BECOMES that
      # frame, spends the cheat code, and the rejection the test was written to prove is never
      # reached. The shape aborts with "next call did not revert as expected" whether the
      # contract is right or wrong, so it carries no signal in either direction and a diff does
      # not show it. It reached main once this way (PR #482, the alignment hook's 1% LP-fee
      # ceiling) past review and a green gate, because the suite it lives in is fork-only. Source
      # scan, so it sits beside INV-1 before the build; --self-test first, same reason.
      - name: expectRevert argument gate
        run: |
          ./scripts/expect-revert-gate.sh --self-test
          ./scripts/expect-revert-gate.sh

      # The compiled tree is keyed on everything the compiler reads: sources, tests, scripts,
      # the config, and the dependency pins. Exact key when this tree was compiled before;
      # otherwise the newest older tree, which forge's per-file cache turns into an incremental
      # compile. The save at job end is under the exact key, which is what the shards wait for.
      - name: Cache key
        id: cache-key
        run: echo "key=forge-${{ hashFiles('contracts/src/**', 'contracts/test/**', 'contracts/script/**', 'contracts/foundry.toml', 'contracts/foundry.v4.toml', 'contracts/scripts/lib-pins.txt') }}" >> "$GITHUB_OUTPUT"

      - name: Restore compiled tree
        uses: actions/cache@v4
        with:
          path: |
            contracts/out
            contracts/cache
          key: ${{ steps.cache-key.outputs.key }}
          restore-keys: |
            forge-

      - name: Build contracts
        run: forge build

      # EIP-170 size/headroom + storage-layout equality gate (noesis-091/142/148/166). Enforces the
      # invariant ERC404BondingStorage.sol documents as CI-checked: ERC404BondingInstance and
      # ERC404BondingOps must have byte-identical storage layouts, since Ops runs in the instance's
      # storage context via delegatecall. A mismatch is a storage-collision bug and must fail the job.
      - name: EIP-170 + storage-layout gate
        run: bash test/factories/erc404/eip170-diet-gate.sh

      # The same ceiling, for every contract that embeds another contract's creation code — in
      # either syntax, since `new X(...)` compiles to the same blob as type(X).creationCode. Four
      # such embeddings sit in a function body and are therefore in the embedder's RUNTIME, against
      # EIP-170: ERC1155Factory, ERC721AuctionFactory, UniTitheHookFactory, and ERC404Factory, which
      # holds DN404Mirror. Those embedders — not the contracts they deploy — are the ones nearest
      # the limit, because the blob is counted in their runtime. Four more sit in a constructor and
      # are in the embedder's CREATION code only, against EIP-3860's initcode cap. The gate prints
      # every margin and the blob that accounts for it, so the room runs out on this log before it
      # runs out in a build; it reads both syntaxes out of src/ and fails on any embedding its table
      # does not measure; and it verifies which budget each blob actually lands in, so a `new X(...)`
      # moving out of a constructor cannot put a contract on the EIP-170 clock in silence.
      - name: EIP-170 embedded-initcode gate
        run: bash test/factories/eip170-embed-gate.sh

      # Every test file is in exactly one shard, or nothing below runs.
      - name: Shard coverage
        run: ./scripts/test-shards-cover.sh

      - name: Read shards
        id: shards
        run: echo "shards=$(jq -c . scripts/test-shards.json)" >> "$GITHUB_OUTPUT"

  test:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.build.outputs.shards) }}
    name: test (${{ matrix.shard.name }})

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: ${{ env.FOUNDRY_VERSION }}

      # The compiled tree is keyed on the sources but forge still reads them, and the
      # cache does not carry lib/. Same pins as the build job, so the same bytes.
      - name: Install contracts/lib deps
        run: ./scripts/install-libs.sh all

      # Exact key only: a shard that had to compile would be paying the cost the build job
      # already paid. fail-on-cache-miss makes that a red step, not a slow one.
      - name: Restore compiled tree
        uses: actions/cache/restore@v4
        with:
          path: |
            contracts/out
            contracts/cache
          key: ${{ needs.build.outputs.cache_key }}
          fail-on-cache-miss: true

      # `ci` profile (foundry.toml): elevated fuzz depth, default invariant depth. The profile
      # MUST be named here — a profile table in foundry.toml that nothing selects has no effect
      # on this step. Invariant depth deliberately stays at the default; see the `deep` profile
      # note in foundry.toml for the measured cost of raising it.
      # Everything else is inherited from `default`, so `skip=[…RealSettlement…]` still keeps the
      # exact-0.8.26 v4 test out of this pinned-0.8.28 compile, and the test/fork/** suites still
      # self-skip at runtime (vm.skip(true) when no fork URL), so no RPC secret is needed.
      # The filter pair is this shard's row of scripts/test-shards.json; an absent field passes
      # no flag.
      - name: Run tests
        env:
          MATCH: ${{ matrix.shard.match }}
          NO_MATCH: ${{ matrix.shard.no_match }}
        run: |
          args=()
          [ -n "$MATCH" ] && args+=(--match-path "$MATCH")
          [ -n "$NO_MATCH" ] && args+=(--no-match-path "$NO_MATCH")
          FOUNDRY_PROFILE=ci forge test "${args[@]}"

  # The real-v4 guards. foundry.v4.toml enables auto_detect_solc so forge selects 0.8.26 for
  # v4-core's real PoolManager. They use an in-memory real PoolManager (not a fork), so they
  # need no RPC. Their own compile under their own config, so they do not wait on the build job.
  #
  # Every test here is in `foundry.toml`'s `skip=` list and therefore runs in NO other job: the
  # default profile cannot compile it. So a proof left out of the set is a proof nothing ever runs
  # again, which is what this step's single-path version had quietly become — the audit proof for
  # the hook's unbound pool key sat green and unrun from the day it was written.
  #
  # The set is NOT written here. It lives in contracts/scripts/real-v4-gate.sh, which is also what
  # a developer runs before pushing, because the two copies of the list had drifted — one file
  # locally where this step ran three — and there is no second copy to keep in step any more.
  # What used to be the standing instruction in this comment, that a file added to `skip=` for a
  # pragma reason belongs in the path the same day, is now an assertion the script refuses to run
  # without: every skipped file must be named there, in its RUN set or in its OUT table with the
  # reason it stays out (`test/audit/FreeMintCurveSolvency.t.sol` is the one entry in that table,
  # red by ruling). It also asks forge which files the path actually reaches, because a
  # `--match-path` that matches nothing exits 0.
  # The job id stays `real-settlement` though the script runs more than RealSettlement: it is a
  # required check, and renaming it silently drops that requirement from branch protection.
  real-settlement:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: ${{ env.FOUNDRY_VERSION }}

      - name: Install contracts/lib deps
        run: ./scripts/install-libs.sh all

      - name: Real-v4 regressions
        run: ./scripts/real-v4-gate.sh
